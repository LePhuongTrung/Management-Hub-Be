export enum BadRequestMessages {
  BAD_REQUEST = 'Bad Request',
  INVALID_ACCRUAL_RATE = 'Invalid accrual rate',
}

export enum UnauthorizedMessages {
  UNAUTHORIZED = 'Unauthorized',
}

export enum ForbiddenMessages {
  FORBIDDEN = 'Forbidden',
}

export enum NotFoundMessages {
  NOT_FOUND = 'Not Found',
}

export enum MethodNotAllowedMessages {
  METHOD_NOT_ALLOWED = 'Method Not Allowed',
}

export enum ConflictMessages {
  CONFLICT = 'Conflict',
}

export enum GoneMessages {
  GONE = 'Gone',
}

export enum UnprocessableEntityMessages {
  UNPROCESSABLE_ENTITY = 'Unprocessable Entity',
}

export enum InternalServerErrorMessages {
  INTERNAL_SERVER_ERROR = 'Internal Server Error',
  INVALID_ROLE_TYPE_KEY = 'Invalid role type key generated.',
}
